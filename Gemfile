source 'https://rubygems.org'

# I think this is how to specify a minimum Ruby version
raise 'Your Ruby version should be >= 2.0.0' unless RUBY_VERSION.to_f > 2.0

# Without this, the blog is nil, nada, nothing.
gem 'jekyll'
gem 'sass'
